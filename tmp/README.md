# 临时工作区（`tmp/`）

本目录用于存放**本地临时文件**与**一次性脚本**（例如探活、草稿 SQL、`bench` 导出中间件等）。除本 `README.md` 外，**其余内容默认不纳入 Git**（见仓库根目录 `.gitignore`）。

## 使用约定

- 勿将含 **Secret / 生产凭据** 的文件长期留在此处；用完即删或移入密钥管理。
- 可复用、可审计的步骤请写入 `docs/aiot/runbooks/`；此处仅作个人沙箱。
- 需要保留给他人的内容请迁入正式路径并提交。
