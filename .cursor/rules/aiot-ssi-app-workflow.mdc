---
description: 默认二开 app ssi_app：本地 apps/ssi_app 推送 GitHub，服务器单向 pull 同步部署
alwaysApply: true
---

# SSI App 二开默认交付路径（ssi_app）

- **适用范围**：自定义 app **`ssi_app`**。当用户**未特别声明**其他方式（例如「只在 bench 上改」「跳过推送」「服务器为准反向同步」等），协作与代理默认按下述流程。
- **单一真相（源码）**：以本仓库根目录 **`apps/ssi_app/`**（独立 Git，父仓库已 `.gitignore` 排除）为编辑与版本入口。
- **默认顺序**：
  1. 在 **`apps/ssi_app`** 内完成变更，执行 **`git commit`** 并 **`git push`** 到远端 **[github.com/clordweel/ssi_app](https://github.com/clordweel/ssi_app)**（分支以团队约定为准，常见 **`develop`**）。
  2. **再在 bench 服务器**上将 **`~/frappe-bench/apps/ssi_app`** 与远端 **单向对齐**：使用 **`git fetch` + `git pull`**（或与 **`bench update`** / CI 发布流程等效、仅自远端取更新的命令）。**不把服务器上的手工改动当作主线再写回 GitHub**，除非用户明确要求迁移或补丁入库。
- **说明**：持续交付场景下「同步」指对已克隆目录 **`pull`**；仅在初次装机或重建目录时用 **`bench get-app`** / **`git clone`**。统称「自远端单向更新」。
- **部署收口**：拉取后按变更执行 **`bench build --app ssi_app`**（涉及前端时）、**`bench migrate`**、按需 **`bench restart`** / **`supervisorctl`**；重大变更前先快照（见 `aiot-core`）。
- **审计**：服务器侧产生写影响的 **`bench`/SSH** 等结束后，仍按 [docs/aiot/audit/README.md](docs/aiot/audit/README.md) 简报；**勿**写入密钥。
- **只读生产凭据**：若服务器仅配置 **read-only** 拉取凭据，则 **`git push`** 永远在本地或具备写权限的环境完成。
