# 变更前快照（占位）

在 PVE 上对目标 VM 做快照后再进行 bench 迁移、大版本升级或危险二开操作。

- **命名**：建议包含日期、`site` 或变更工单缩写。
- **具体命令**：依集群使用 `qm` / `pct` 等，由运维按 PVE 版本与策略补全；此处不绑定单一语法。

与 [audit/README.md](../audit/README.md) 配合：**执行快照动作后**可在 audit 简报中记一笔「已快照 + 名称/ID（可写短 ID）」。
