# PVE 虚拟机环境（模板）

复制本文件为 `pve-vm.local.md` 后填写真实值；**勿将 `*.local.md` 提交到 Git**（已在 `.gitignore` 中忽略）。

| 字段 | 示例 / 说明 |
|------|-------------|
| 显示名 | |
| VMID | |
| 管理网 IP | |
| SSH 别名或 `user@host` | |
| 是否生产 | 是 / 否 |
| bench Linux 用户 | |
| bench 家目录 / frappe-bench 路径 | 如 `~/frappe-bench` |
| 备注 | HA、快照策略、维护窗口 |
