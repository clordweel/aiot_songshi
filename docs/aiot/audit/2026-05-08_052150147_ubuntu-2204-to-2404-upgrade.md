## 2026-05-08 10:30:00 — Ubuntu 22.04 → 24.04 LTS（生产前置）

- 时间：2026-05-08 10:30:00 CST (UTC+8)（会话执行；补录，同日排序）
- 环境：VM `10.0.0.40`（hostname：`aiot-songshi-frappe`），SSH `root`（密钥）
- 类型：`deploy`（操作系统发行版升级）
- 摘要：
  - 在 22.04 上执行 `apt upgrade` / `full-upgrade` 与内核就绪重启；
  - 执行 `RELEASE_UPGRADER_NO_SCREEN=1 do-release-upgrade -f DistUpgradeViewNonInteractive`，远端日志：`/root/release-upgrade.log`；
  - 升级完成后重启加载 **Noble** 内核；
  - 验收：`lsb_release` → Ubuntu **24.04.4 LTS**，`uname -r` → **6.8.0-111-generic**，`/var/run/reboot-required` 已清除。
- 结果：`ok`
- 备注：**请在 PVE 侧保留本次变更前后快照策略**（重大变更）；主机上仍装有 Docker 等与 ERPNext 无关组件，后续 bench 生产步骤另行审计。
- 审批（可选）：
- 审批时间（可选）：
