## 2026-05-08 16:20:00 — ssi_app GitHub SSH origin + 推送 develop

- 时间：2026-05-08 16:20:00 CST (UTC+8)（会话执行；补录，同日排序）
- 环境：VM **10.0.0.40**，用户 **frappe**，路径 **`~/frappe-bench/apps/ssi_app`**
- 类型：`deploy`（git）
- 摘要：`origin` 由 HTTPS 改为 **`git@github.com:clordweel/ssi_app.git`**；在 **`~/.ssh/config`** 中为 **`Host github.com`** 指定 **`IdentityFile ~/.ssh/id_ed25519_github_clordweel`**；提交 **`feat: modules ssi_app, ssi_stock, ssi_accounts; update modules.txt`**（含 `modules.txt` 与 `ssi_stock`/`ssi_accounts` 包）；**`git push -u origin develop`** 成功，远端新建 **`develop`** 跟踪分支。
- 结果：`ok`
- 审批（可选）：
- 审批时间（可选）：
